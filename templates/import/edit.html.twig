{% extends 'base.html.twig' %}

{% block title %}Edit Import{% endblock %}

{% block body %}
    <h1>Edit Import</h1>

    {{ include('import/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_import_index') }}">back to list</a>

    {{ include('import/_delete_form.html.twig') }}
{% endblock %}

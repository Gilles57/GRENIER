{% extends '/base_admin.html.twig' %}

{% block title %}Oeuvre index{% endblock %}

{% block body %}
    <h1>Toutes les œuvres</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for oeuvre in oeuvres %}
            <tr>
                <td>{{ oeuvre.id }}</td>
                <td>{{ oeuvre.name }}</td>
                <td>{{ oeuvre.description }}</td>
                <td>
                    <a href="{{ path('app_admin_oeuvre_show', {'id': oeuvre.id}) }}">show</a>
                    <a href="{{ path('app_admin_oeuvre_edit', {'id': oeuvre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_oeuvre_new') }}">Create new</a>
{% endblock %}

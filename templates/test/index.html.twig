{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1> {{ filmFinded.title }}</h1>
            <h5>{{ filmFinded.tagline }}</h5>
            <hr>
        </div>
        <div class="row">
            <div class="col-4">
                <h4><span class="fw-bold">Id : </span>{{ id }} - <span
                            class="fw-bold">Imdb_id : </span>{{ filmFinded.imdb_id }}</h4>
                <div class="imgBx">
                    <img class="test"
                         src="{{ asset('/uploads/affiches'~affiche ) }}"
                         alt="Card image Informatique"
                         style="height: 10rem">
                </div>
                <br>
                <p><span class="fw-bold">Résumé : </span>{{ filmFinded.overview }}</p>
            </div>
            <div class="col-4">
                <p><span class="fw-bold">Adultes :  </span>{{ filmFinded.adult }}</p>
                {% if  filmFinded.belongs_to_collection is not null %}
                    <p><span class="fw-bold">Collection  : </span>{{ filmFinded.belongs_to_collection.name }}</p>
                {% endif %}
                <p><span class="fw-bold">Genres : </span>
                    {% for genre in filmFinded.genres %}
                        {{ genre.name }},
                    {% endfor %}</p>
                <p><span class="fw-bold">Homepage : </span>{{ filmFinded.homepage }}</p>
                <p><span class="fw-bold">Langue originale : </span>{{ filmFinded.original_language }}</p>
                <p><span class="fw-bold">Titre original : </span>{{ filmFinded.original_title }}</p>

                <p><span class="fw-bold">Popularité : </span>{{ filmFinded.popularity }}</p>
                <p><span class="fw-bold">Compagnies : </span>
                    {% for cie in filmFinded.production_companies %}
                        {{ cie.name }},
                    {% endfor %}</p>
            </div>
            <div class="col-4">
                <p><span class="fw-bold">Pays : </span>
                    {% for pays in filmFinded.production_countries %}
                        {{ pays.name }},
                    {% endfor %}</p>
                <p><span class="fw-bold">Date de sortie : </span>{{ filmFinded.release_date }}</p>
                <p><span class="fw-bold">Revenue : </span>{{ filmFinded.revenue }}</p>
                <p><span class="fw-bold">Runtime ??? : </span>{{ filmFinded.runtime }}</p>
                <p><span class="fw-bold">Langues : </span>
                    {% for langue in filmFinded.spoken_languages %}
                        {{ langue.name }},
                    {% endfor %}</p>
                <p><span class="fw-bold">Status : </span>{{ filmFinded.status }}</p>
                <p><span class="fw-bold">Video : </span>{{ filmFinded.video }}</p>
                <p><span class="fw-bold">Moyenne des votes : </span>{{ filmFinded.vote_average }}</p>
                <p><span class="fw-bold">Nb de votes : </span>{{ filmFinded.vote_count }}</p>
            </div>
        </div>
    </div>
{% endblock %}
